name: drag_grid_list
description: "Reorderable list + responsive grid with drag & drop, reordering, and cross-group drag between multiple lists/grids. Long-press drag on mobile, mouse drag on desktop/web."
version: 0.0.3
homepage: https://github.com/Androidsignal/drag_grid_list
repository: https://github.com/Androidsignal/drag_grid_list
issue_tracker: https://github.com/Androidsignal/drag_grid_list/issues
topics:
  - widgets

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
